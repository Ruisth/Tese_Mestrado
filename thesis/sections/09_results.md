<!-- GENERATED FILE - DO NOT EDIT.
     Source: latex/chapters/05_evaluation.tex
     Regenerate with: python thesis/tools/generate_sections.py
     The LaTeX tree under thesis/latex/ is the single edited source. -->

# Evaluation and Discussion

This chapter reports the experimental evaluation of the gateway against the protocol frozen in Section . Every quantitative statement in this chapter resolves to a raw-data directory with its manifest and checksums, and is regenerated by the analysis pipeline; the claim–evidence matrix in the repository records this mapping per claim.

## Measurement Methodology

Latency is measured entirely within the controller process: a monotonic-clock reading at the MQTT receive callback and a second reading after the Ditto acknowledgement, with the difference converted to milliseconds. Using a single process and a monotonic clock avoids clock synchronisation between machines for the primary metric. End-to-end accounting (sent, received, duplicate, rejected, confirmed, lost) joins the simulator’s send log with the controller’s event log on message identifiers, which are deterministic per run. Resource consumption is sampled every second per container. The statistical unit is the run; per-condition aggregates report mean, standard deviation and 95% confidence intervals, with medians and percentiles for skewed distributions, as fixed in Section .

## Experimental Setup

**\[TODO: pending data-v1 — record from the campaign manifests: cloud provider and region of the ARM64 VM, exposed CPU model and vCPU count, memory and disk, kernel and operating-system versions, the shared-vCPU limitation, container image digests, repository commit, and the exact simulator host placement. No value may be written here that is not present in a manifest under `results/raw/]`.**

## Platform Reproducibility and Readiness

This section answers the functional part of RQ1.

### Image Build and Boot Consistency

**\[TODO: pending data-v1 — clean-checkout build outcome, five QEMU boot results, container-runtime smoke evidence.\]**

### Stack Time-to-Readiness

**\[TODO: pending data-v1 — ten cold starts: readiness times with run-level statistics.\]**

### Twin Creation

**\[TODO: pending data-v1 — ten independent twin creations: outcomes.\]**

## Correctness and Reliability

This section answers RQ2.

### Nominal Scenario

**\[TODO: pending data-v1 — ten nominal runs: accepted/rejected/duplicate/lost counts and delivery rate per run; final twin-state verification for the three devices.\]**

### Fault Injection: Invalid Payloads

**\[TODO: pending data-v1 — invalid-payload scenario: correct-rejection accounting, no contamination of twins.\]**

### Fault Injection: Dropout and Reconnect

**\[TODO: pending data-v1 — dropout-reconnect scenario: recovery behaviour, duplicate handling after reconnect, event accounting.\]**

## Performance

This section answers RQ3.

### Latency

**\[TODO: pending data-v1 — nominal latency p50/p95/p99 per run with run-level aggregates; distribution figures generated by the analysis script.\]**

### Throughput and Saturation

**\[TODO: pending data-v1 — load-sweep results at 10/50/100/250 msg/s; identify the operational saturation point per the frozen definition (loss above 1%, persistent queue growth, p95 above one second, or sustained CPU above 90% for 60 seconds).\]**

### Resource Consumption

**\[TODO: pending data-v1 — per-container CPU and RAM profiles across load levels.\]**

### Stability: 24-Hour Soak

**\[TODO: pending data-v1 — descriptive soak analysis: continuity of service, resource trends, absence or presence of unrecovered failures. No confidence interval is computed for the single soak run.\]**

## Answers to the Research Questions

### RQ1

**\[TODO: pending data-v1 — answer RQ1 strictly from the platform evidence above.\]**

### RQ2

**\[TODO: pending data-v1 — answer RQ2 strictly from the correctness and reliability evidence above.\]**

### RQ3

**\[TODO: pending data-v1 — answer RQ3 strictly from the performance evidence above, stating the observed trade-offs and their operational envelope.\]**

## Comparison with Prior Work

**\[TODO: pending data-v1 — qualitative comparison of the evaluated scope against the published C2DTA evaluation (single smartwatch profile, x86 virtual machine) and against edge digital-twin evaluations such as ; strictly no numeric cross-paper comparisons unless measurement conditions are demonstrably comparable.**

## Threats to Validity

The following threats are identified at protocol time; the discussion is revisited once results exist.

- **Internal validity.** Instrumentation shares the process with the measured code; the event-log write path adds work per message. Mitigations: monotonic clocks, single-process latency measurement, warm-up exclusion, and identical instrumentation across all conditions. The cloud VM’s shared vCPUs may introduce neighbour-induced variance; mitigations: repeated runs, randomised load-sweep order, and reporting of dispersion, with the limitation stated explicitly.

- **External validity.** Telemetry is synthetic and the deployment is a single gateway with three devices on one ARM64 cloud VM; results do not automatically transfer to physical single-board hardware, other device populations or other stacks. Emulated-platform evidence is functional only.

- **Construct validity.** The primary latency metric covers the controller’s processing path (receive to twin acknowledgement), not the device-to-gateway network leg; delivery-rate definitions count deliberately invalid payloads as rejections, not losses. These definitions are fixed in Section  and applied uniformly.

- **Conclusion validity.** The statistical unit is the run; confidence intervals use the per-run distribution, and the single soak run is analysed descriptively without inferential statistics.
