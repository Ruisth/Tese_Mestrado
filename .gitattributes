* text=auto eol=lf
*.sh text eol=lf
*.py text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.json text eol=lf
*.md text eol=lf
*.bb text eol=lf
*.bbappend text eol=lf
*.conf text eol=lf
*.service text eol=lf
*.network text eol=lf
*.png binary
*.jpg binary
*.pdf binary

# Preserve the serial-console byte stream in the strict G1 evidence capsule.
# PTY carriage returns are part of the instrumentation record and must not be
# normalised by Git before SHA-256 verification. Captured/generated evidence
# also keeps source whitespace, so diff whitespace diagnostics do not rewrite
# or reject those immutable bytes.
docs/evidence/g1-yocto-qemu/2026-08-14-clean-build-f0e19d5/* whitespace=-blank-at-eol,-blank-at-eof
docs/evidence/g1-yocto-qemu/2026-08-14-clean-build-f0e19d5/qemu-*.log -text

# Preserve compiler output bytes in the Word import verification capsule.
docs/evidence/word-import-2026-09-17/*.log -text whitespace=-blank-at-eol,-blank-at-eof
docs/evidence/word-import-2026-09-17/*.blg -text whitespace=-blank-at-eol,-blank-at-eof

# Preserve the captured bytes of the integrated QEMU evidence capsules: the
# console transcripts keep the carriage returns and escape sequences of the
# pseudo-terminal, and every file is covered by a SHA-256 manifest.
docs/evidence/integrated-qemu/** -text whitespace=-blank-at-eol,-blank-at-eof,-space-before-tab

# Preserve the captured bytes of the G2 complete-flow capsule: the console
# transcripts of the guest session keep the carriage returns of the
# pseudo-terminal, and every file is covered by a SHA-256 manifest.
docs/evidence/g2-complete-flow/** -text whitespace=-blank-at-eol,-blank-at-eof,-space-before-tab
